# PocketWindow Control Agent - Python 依赖

# WebRTC. Keep PyAV pinned to a Windows wheel for Python 3.9; newer PyAV
# releases may fall back to a local FFmpeg/C++ build.
av==14.2.0
aiortc==1.13.0

# Windows API
pywin32>=306

# 图像处理
Pillow>=10.0.0
opencv-python>=4.8.0
numpy>=1.24.0

# 其他工具
requests>=2.31.0
pyperclip>=1.8.0

# plain WebSocket client
websocket-client>=1.6.0

# 信令客户端 (可选，用于旧版 Socket.IO 方案)
socket.io-client>=0.7.1
PySide6>=6.11.0
