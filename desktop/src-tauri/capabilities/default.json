{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Enable the desktop window to access Tauri core APIs and project commands.",
  "windows": [
    "main"
  ],
  "permissions": [
    "core:default",
    "shell:default"
  ]
}
